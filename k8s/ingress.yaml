apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-attendance-ingress
spec:
  rules:
  - host: flask.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: flask-attendance-service
            port:
              number: 80
